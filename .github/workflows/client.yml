name: client
on:
  push:
    branches:
      - reference
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout source code.
        uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
        working-directory: ./my-agora-states-client
      - name: Build
        run: npm build
        working-directory: ./my-agora-states-client
      - name: SHOW AWS CLI VERSION
        env:
          AWS_ACCESS_KEY_ID: ${{ AKIASEBV4CULN2J7D7WS }}
          AWS_SECRET_ACCESS_KEY: ${{ 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 }}
          AWS_EC2_METADATA_DISABLED: true
        run: |
          aws --version
      - name: Sync Bucket
        env:
          AWS_ACCESS_KEY_ID: ${{ AKIASEBV4CULN2J7D7WS }}
          AWS_SECRET_ACCESS_KEY: ${{ 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 }}
          AWS_EC2_METADATA_DISABLED: true
        run: |
          aws s3 arn:aws:s3:::fe-108-wusi-hub \
            --region ap-northeast-2 \
            build s3://<?> \
            --delete
        working-directory: <?>

